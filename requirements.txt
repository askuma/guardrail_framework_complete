# Guardrail Framework - Core Dependencies

# Web server (required for Docker deployment)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Async support
aiofiles>=23.0.0

# Optional: Monitoring
prometheus-client>=0.18.0
python-json-logger>=2.0.0

# Optional: Distributed caching
redis>=5.0.0

# Optional: DB-backed audit log
sqlalchemy>=2.0.0

# Dev / testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
