# ── Web server (required) ─────────────────────────────────────────────────────
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# ── Async support ─────────────────────────────────────────────────────────────
aiofiles>=23.0.0

# ── Persistence ───────────────────────────────────────────────────────────────
sqlalchemy>=2.0.0
alembic>=1.12.0
# For PostgreSQL in production, also install: psycopg2-binary>=2.9.0

# ── Monitoring ────────────────────────────────────────────────────────────────
prometheus-client>=0.18.0
python-json-logger>=2.0.0

# ── Optional: Real Presidio PII detection ─────────────────────────────────────
# Uncomment to activate the real Presidio SDK backend (replaces regex fallback)
# presidio-analyzer>=2.2.0
# presidio-anonymizer>=2.2.0
# spacy>=3.0.0
# After installing spacy run: python -m spacy download en_core_web_lg

# ── Optional: Redis (multi-instance rate limiting + caching) ──────────────────
# Set GUARDRAIL_REDIS_URL=redis://host:6379/0 to activate.
# redis>=5.0.0

# ── Report signing ────────────────────────────────────────────────────────────
# PDF generation and digital signing for red-team reports
# (GET /redteam/reports/{run_id}/export)
reportlab>=4.0.0
pyhanko>=0.21.0
pyhanko-certvalidator>=0.26.0

# ── Dev / testing ─────────────────────────────────────────────────────────────
pytest>=7.0.0
pytest-asyncio>=0.21.0
httpx>=0.24.0        # needed by pytest for FastAPI test client
